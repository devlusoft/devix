# devix

devix es un meta-framework de SolidJS con enrutamiento basado en archivos, SSR con streaming y tipado end-to-end entre cliente y servidor.

devlusoft mantiene devix. Está publicado como MIT para que devs externos puedan mantener proyectos de clientes construidos con devix.

Para arrancar: `pnpm install` y `pnpm test`.